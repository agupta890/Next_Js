export default function AdminLayout({children}){
    return(
        <html>
            <body>
                <h1>Admin Layout</h1>
                {children}
            </body>
        </html>
    )
}